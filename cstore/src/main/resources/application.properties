# Application name (must match the gateway route)
spring.application.name=store-service

# Fixed port
server.port=8082

# MongoDB connection
spring.data.mongodb.uri=mongodb://localhost:27017/cstore

# Disable Spring Security (for development)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Eureka client configuration
eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka/}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.enabled=true
spring.cloud.service-registry.auto-registration.enabled=true

# Optional: Groq API key (if used)
groq.api.key=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